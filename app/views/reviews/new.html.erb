<div class="container">
<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back", booking_path(@booking), class: "btn btn-primary" %>
<%= link_to "Home", sloths_path, class: "btn btn-primary" %>
</div>

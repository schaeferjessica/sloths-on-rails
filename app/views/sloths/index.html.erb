<div class= "container">
  <h1>Check out these great sloths!</h1>
  <p class="emoji">🦥🦥🦥<p>
  <%= link_to "add a sloth", new_sloth_path, class: "btn" %>
  <p>Book your sleepy friend today.</p>
  <div class="cards">
    <% @sloths.each do |sloth| %>
      <!-- <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<% sloth.photo %>)"> -->
        <%= cl_image_tag sloth.photos.first.key, class:"card-category" %>
      <%= link_to sloth.name, sloth_path(sloth) %>
    </div>
  <% end %>
</div>

<div id="map", class = "mt-3",
     style="width: 100%; height: 400px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>
